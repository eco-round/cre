###############################################################################
### CRE Workflow Environment Variables                                       ###
### DO NOT COMMIT THE REAL .env FILE — IT CONTAINS PRIVATE KEYS             ###
###############################################################################

# EVM private key used for on-chain writes (lockMatch / resolveMatch)
# Must be the oracle address set on each VaultMatch contract
CRE_ETH_PRIVATE_KEY=0xyour-private-key-here

# Default CRE target when --target flag is omitted
CRE_TARGET=production-settings

# Confidential HTTP API keys — injected into source requests inside the CRE enclave
# In production, store these in the Chainlink DON Vault via: cre secrets set
# For local simulation, these are loaded from cre/.env and override secrets.yaml
PANDASCORE_API_KEY_ALL=pandascore123ECO
VLR_API_KEY_ALL=vlr123ECO
LIQUIPEDIA_API_KEY_ALL=liquipedia123ECO
